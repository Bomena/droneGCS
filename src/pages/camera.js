import React, { useState } from "react";

export default function OpenWindow() {

let image = document.getElementById('ig').src

function OpenWindow(){
    window.open('camera.html', 'width=640, height=480, resizable=1, scrollbars=0, toolbar=0, status=0, menubar=0, top=0, right=0');
}

}